**CRITICAL — zero tolerance:** Ticket numbers MUST use the `T-` prefix. The `#` prefix is **FORBIDDEN**.

- Correct: `T-1`, `T-351`, `T-272`
- Wrong: `#1`, `#351`, `#272`

This applies to ALL output — confirmations, references, commit messages, PR titles, logs, and any other text that mentions a ticket number. No exceptions. Every single time you output a ticket number, it MUST start with `T-`.

**Self-check before every response:** If your output contains `#` followed by a number that refers to a ticket, STOP and replace it with `T-` before responding.
